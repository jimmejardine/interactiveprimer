# sounds/ — UI audio

The quiz's feedback sounds (`quiz-pass.wav`, `quiz-fail.wav`), played by the framework
(`src/sounds.ts`). Kept tiny and served as-is; MathLive's own keypress sounds are disabled in
favour of these.
