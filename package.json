{
  "name": "interactiveprimer",
  "version": "0.0.1",
  "description": "Interactive Primer - smart web pages teaching maths, physics and CS as a tree of knowledge.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "typecheck": "tsc -p jsconfig.json",
    "graph": "node scripts/build-graph.js",
    "check:graph": "node scripts/build-graph.js --check",
    "check": "npm run typecheck && npm test && npm run check:graph",
    "serve": "node scripts/serve.js"
  },
  "devDependencies": {
    "@types/katex": "^0.16.7",
    "@types/node": "^22.5.0",
    "katex": "^0.16.11",
    "manim-web": "^0.3.18",
    "typescript": "^5.5.0"
  },
  "license": "MIT"
}
