node_modules/
*.log
.DS_Store
/.shots
/.shots7
/.shotsB
/scripts/creator-videos/work/

# Framework build outputs (regenerated by `npm run build`; built in CI on deploy).
# All source lives in src/ (TypeScript); js/ holds only the generated classic scripts
# (boot.js with the hash stamped in, analytics.js) and sw.js is the generated service worker.
/dist/
/js/
/sw.js
