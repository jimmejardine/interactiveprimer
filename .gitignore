node_modules/
*.log
.DS_Store
/.shots*
/scratchpad/
/scripts/creator-videos/work/

# Framework build outputs (regenerated by `npm run build`; built in CI on deploy).
# All source lives in src/ (TypeScript); the build emits the bundles + generated classic
# scripts (boot.js, analytics.js) into /dist/ and the service worker to /sw.js.
/dist/
/sw.js
# Served standalone pages, generated from their src/*.html templates (English stamped in from en.ts).
/index.html
/course.html
/course-quiz.html
